import { beatmap } from 'ojsama'
import { ParsedCatchResult } from '../objects/catch/parsedCatchResult'

export const STAR_SCALING_FACTOR = 0.153

export const calculateMaxCombo = (
  map: beatmap,
  parsedCatchResult: ParsedCatchResult
): number => {
  return 0
}
